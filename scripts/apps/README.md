# App Installers

Store one installer per app in this directory.

Conventions:
- Name each installer as `<app>.sh`
- Keep each installer focused on a single app
- Run installers through `./scripts/main.sh install <app>`
